name: Deploy CC Portal

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  # ── Job 1: Build frontend on GitHub-hosted runner, deploy to Linux EC2 ──
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build frontend
        working-directory: acdb-api/frontend
        run: |
          npm ci
          npm run build
          echo "Frontend built: $(find dist -type f | wc -l) files"

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 30 -H ${{ secrets.EC2_LINUX_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Deploy frontend to Linux EC2
        run: |
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.EC2_LINUX_HOST }} "sudo rm -rf /opt/cc-portal/frontend/*"
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            acdb-api/frontend/dist/ \
            ubuntu@${{ secrets.EC2_LINUX_HOST }}:/tmp/cc-frontend/
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.EC2_LINUX_HOST }} \
            "sudo cp -r /tmp/cc-frontend/* /opt/cc-portal/frontend/ && sudo chown -R root:root /opt/cc-portal/frontend/ && rm -rf /tmp/cc-frontend"
          echo "Frontend deployed to Linux EC2"

      - name: Verify frontend
        run: |
          sleep 2
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://cc.1pwrafrica.com/)
          echo "Frontend HTTP status: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "WARNING: Frontend returned $STATUS"
            exit 1
          fi

  # ── Job 2: Deploy backend to Linux EC2 (replaces Windows self-hosted runner) ──
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 30 -H ${{ secrets.EC2_LINUX_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      # Install the HR Portal credentials as a systemd drop-in for both API
      # services. Previously the backend called /api/employees/lookup with no
      # X-API-Key, which only worked because the HR endpoint had a fail-open
      # auth bug. HR now enforces the key; see hr_portal commit 42fab508.
      # The drop-in is re-written on every deploy (install -m 600) so
      # rotations are just `gh secret set HR_PORTAL_API_KEY` + rerun.
      - name: Install HR Portal systemd drop-in
        env:
          HR_PORTAL_URL: ${{ secrets.HR_PORTAL_URL }}
          HR_PORTAL_API_KEY: ${{ secrets.HR_PORTAL_API_KEY }}
        run: |
          if [ -z "$HR_PORTAL_API_KEY" ]; then
            echo "SKIP: HR_PORTAL_API_KEY not set in GH secrets — backend lookup_employee() will fail"
            exit 0
          fi
          cat > /tmp/hr-api.conf <<EOF
          [Service]
          Environment="HR_PORTAL_URL=$HR_PORTAL_URL"
          Environment="HR_PORTAL_API_KEY=$HR_PORTAL_API_KEY"
          EOF
          scp -i ~/.ssh/deploy_key /tmp/hr-api.conf \
            ubuntu@${{ secrets.EC2_LINUX_HOST }}:/tmp/hr-api.conf
          # Iterate over services that exist on this host. ZM (1pdb-api-zm) is
          # optional — only install the drop-in if the unit is present.
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.EC2_LINUX_HOST }} '
            set -e
            for svc in 1pdb-api 1pdb-api-bn 1pdb-api-zm; do
              if systemctl list-unit-files | grep -q "^${svc}\.service"; then
                sudo mkdir -p /etc/systemd/system/${svc}.service.d
                sudo install -m 600 -o root -g root /tmp/hr-api.conf \
                  /etc/systemd/system/${svc}.service.d/hr-api.conf
                echo "HR API drop-in installed on ${svc}"
              else
                echo "Skipping ${svc} (unit not present on this host)"
              fi
            done
            sudo systemctl daemon-reload
            rm -f /tmp/hr-api.conf
          '
          rm -f /tmp/hr-api.conf

      - name: Deploy backend to Linux EC2
        run: |
          # Sync Python backend files (exclude frontend, venv, caches, db files)
          rsync -avz --delete \
            --exclude='frontend/' \
            --exclude='venv/' \
            --exclude='__pycache__/' \
            --exclude='.mypy_cache/' \
            --exclude='node_modules/' \
            --exclude='.env' \
            --exclude='*.accdb' \
            --exclude='cc_auth.db' \
            --exclude='firebase-service-account.json' \
            --exclude='*.pyc' \
            --exclude='*.ldb' \
            -e "ssh -i ~/.ssh/deploy_key" \
            acdb-api/ \
            ubuntu@${{ secrets.EC2_LINUX_HOST }}:/tmp/cc-backend/

          # Move to final location and set permissions
          # Script is POSIX sh (no `local`) so it works if SSH uses /bin/sh (dash).
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.EC2_LINUX_HOST }} << 'DEPLOY_SCRIPT'
            sudo rsync -a /tmp/cc-backend/ /opt/cc-portal/backend/
            sudo chown -R cc_api:cc_api /opt/cc-portal/backend/
            rm -rf /tmp/cc-backend

            # Install Python dependencies if requirements changed
            cd /opt/cc-portal/backend
            if [ -f venv/bin/pip ]; then
              sudo -u cc_api venv/bin/pip install -r requirements.txt --quiet 2>/dev/null
            fi

            # Apply incremental CC migrations only (010_*.sql, 011_*.sql, …).
            # Do NOT run 001–009 here: legacy ops are one-time DBA/ops.
            # Use postgres OS user + local DB name: cc_api is not table owner (ALTER fails).
            MIG_DIR=/opt/cc-portal/backend/migrations
            apply_incremental() {
              _db="$1"
              INCR=$(find "$MIG_DIR" -maxdepth 1 -name '0[1-9][0-9]_*.sql' -type f | sort -V)
              for f in $INCR; do
                [ -f "$f" ] || continue
                echo "Applying $(basename "$f") to database $_db..."
                sudo -u postgres psql -d "$_db" -v ON_ERROR_STOP=1 -f "$f" || exit 1
              done
            }
            apply_incremental onepower_cc
            if sudo -u postgres psql -tAc "SELECT 1 FROM pg_database WHERE datname='onepower_bj'" 2>/dev/null | grep -q 1; then
              apply_incremental onepower_bj
            else
              echo "Skipping onepower_bj (database not present on this host)"
            fi
            # Zambia DB is optional until ZEDSI sites are commissioned (per
            # docs/sop-add-new-country.md). Apply migrations only if the DB
            # exists; the unit / Caddy route are stood up by ops separately.
            if sudo -u postgres psql -tAc "SELECT 1 FROM pg_database WHERE datname='onepower_zm'" 2>/dev/null | grep -q 1; then
              apply_incremental onepower_zm
            else
              echo "Skipping onepower_zm (database not present on this host)"
            fi

            # Restart API services that exist on this host (LS :8100, BN :8101, optional ZM :8102).
            for svc in 1pdb-api 1pdb-api-bn 1pdb-api-zm; do
              if systemctl list-unit-files | grep -q "^${svc}\.service"; then
                sudo systemctl restart "$svc"
                echo "Restarted ${svc}"
              else
                echo "Skipping ${svc} restart (unit not present)"
              fi
            done
            echo "Backend deployed"
          DEPLOY_SCRIPT

      - name: Health check (LS)
        run: |
          echo "Waiting for services to start..."
          sleep 8
          for i in 1 2 3; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://cc.1pwrafrica.com/api/health)
            echo "LS health check attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              curl -s https://cc.1pwrafrica.com/api/health | python3 -m json.tool
              exit 0
            fi
            sleep 5
          done
          echo "WARNING: LS health check failed after 3 attempts"
          exit 1

      - name: Health check (BN)
        run: |
          for i in 1 2 3; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://cc.1pwrafrica.com/api/bn/health)
            echo "BN health check attempt $i: HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              curl -s https://cc.1pwrafrica.com/api/bn/health | python3 -m json.tool
              exit 0
            fi
            sleep 5
          done
          echo "WARNING: BN health check failed after 3 attempts"
          exit 1

      # ZM is optional — health check passes silently if /api/zm/health is
      # not yet routed through Caddy. Once Zambia goes live this becomes a
      # hard check (mirror BN above by removing the 404 short-circuit).
      - name: Health check (ZM, optional)
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://cc.1pwrafrica.com/api/zm/health || echo 000)
          echo "ZM health check: HTTP $STATUS"
          if [ "$STATUS" = "200" ]; then
            curl -s https://cc.1pwrafrica.com/api/zm/health | python3 -m json.tool
          elif [ "$STATUS" = "404" ] || [ "$STATUS" = "000" ]; then
            echo "ZM API not yet deployed on this host — skipping (per SOP)"
          else
            echo "WARNING: ZM endpoint reachable but returned $STATUS"
            exit 1
          fi
